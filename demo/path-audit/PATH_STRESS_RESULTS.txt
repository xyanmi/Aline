Path handling stress-check findings (real host, Windows local side)

What I tested end-to-end against yantw-novpn:
- push with absolute local path -> remote ~/... (repeated 5x)
- push with relative local path -> remote ~/...
- push with absolute local path -> remote /tmp/...
- push/pull with spaces in local and remote paths
- merge vs mirror semantics after adding remote_only.txt remotely
- pull to absolute local path and relative local path
- sync start/stop with absolute local path
- sync start/stop with relative local path
- negative cases: remote path supplied as C:/... and C:\Users\...

Observed transfer backend:
- tar+ssh in every real run (local rsync unavailable)

Functional outcome:
- No wrong remote C:... directories were created in the tested push flows.
- Absolute local path push roundtrip: OK
- Relative local path push roundtrip: OK
- /tmp remote destination roundtrip: OK
- Space-containing local+remote path roundtrip: OK
- merge kept remote_only.txt: OK
- mirror removed remote_only.txt: OK
- pull to absolute local path: OK
- pull to relative local path: OK
- sync with absolute local path propagated marker: OK
- sync with relative local path propagated marker: OK
- Windows-style remote paths are now rejected explicitly instead of being passed through.

What I fixed in code during this pass:
1. Windows tar+ssh push source path normalization
   - src/sync/rsync.js
   - push tar creation now normalizes the local Windows path before `tar -C`
2. Remote path validation for Unix-like targets
   - src/sync/rsync.js
   - reject remote paths like C:/... or containing backslashes
3. rsync branch local-path normalization symmetry
   - src/sync/rsync.js
   - push/pull rsync path construction now uses normalized local paths too
4. Regression tests added/expanded
   - tests/rsync.test.js

Remaining concerns after the audit:
- The Windows/Git Bash remote path normalization in src/cli/commands.js is heuristic and focused on ~/aline-test and /tmp patterns; it is good for the intended workflows but not a universal Windows-shell path parser.
- Docs/skill wording around path usage could still be tightened so users prefer Unix-style remote paths like ~/... or /tmp/... and never C:/... for remote targets.

Bottom line:
- I stress-tested the critical path bug you reported and adjacent variants pretty hard.
- The dangerous class of bug (remote creation of bogus C:... directories from Windows local usage) appears fixed for the tested push/pull/sync workflows.
- The most important remaining action is to commit and publish these fixes as the next patch release.
